# Ataque 1 - Inyección SQL (SQLi)

## 1. Evidencia del Ataque
El ataque lo ejecuté en la sección "SQL Injection" del portal. En el cuadro de texto ingresé el siguiente payload[cite: 2, 4]:

```sql
' OR '1'='1